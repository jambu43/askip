let initialState = {
  notifications: {},
  notifications_loading: false,
};

export const notificationReducers = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
